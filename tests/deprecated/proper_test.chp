load("lib/libprint.chh");

var testVar = "Test 1";
Println(testVar);

func test(){
    # Should access global
    Println(testVar);
    # Local declaration
    var testVar = "Test 2";
    # Should print local
    Println(testVar);
}

test();
# Should still be global
Println(testVar);